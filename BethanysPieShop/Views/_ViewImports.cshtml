@*ViewImports file lets you add namespaces so that they will be known in all other views within the Views folder. This means that we can import models without specifying the namespace, eg:
    instead of @model BethanysPieShop.ViewModels.PiesListViewModel we can use @model PiesListViewModel (just the class name) *@

@using BethanysPieShop.ViewModels